/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    swcMinify: true,
    experimental:{
      reactRefresh: true,
    }

  };
  
  export default nextConfig;

  